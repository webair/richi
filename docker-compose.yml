services:
  mqtt-broker:
    container_name: mqtt-broker
    build: ./mqtt-broker
    environment:
      USERNAME: ${MQTT_BROKER_USERNAME}
      PASSWORD: ${MQTT_BROKER_PASSWORD}
    ports:
      - "1883:1883"
    restart: no

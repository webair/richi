services:
  mqtt-broker:
    image: eclipse-mosquitto:2.0.22-openssl
    container_name: mqtt-broker
    ports:
      - "1883:1883"
    volumes:
      - ./mosquitto/config:/mosquitto/config
    restart: no
